--- src/components/FleetManagerUI.jsx
+++ src/components/FleetManagerUI.jsx
@@ -413,10 +413,10 @@
                                                         if (e.target.closest('button')) return;
                                                         if (onTaskClick) onTaskClick(task, repo.name);
                                                     }}>
-                                                    <div className="flex items-start gap-2 flex-1">
+                                                    <div className="flex items-start gap-2 flex-1 min-w-0">
                                                         <div className="text-gold9/40 font-mono text-[10px] mt-0.5 w-4">{i+1}.</div>
-                                                        <div className="flex flex-col">
-                                                            <div className={`text-xs ${textStyle} line-clamp-2`}>{task.prompt}</div>
+                                                        <div className="flex flex-col flex-1 min-w-0">
+                                                            <div className={`text-xs ${textStyle} truncate`}>{task.prompt}</div>
                                                             <div className="flex gap-2 mt-1">
                                                                 <span className={`text-[9px] font-mono ${statusTextColor}`}>{statusText}</span>
                                                                 {dependencyLabel && <span className="text-[9px] font-mono text-orange-500/50">{dependencyLabel}</span>}
@@ -424,7 +424,7 @@
                                                         </div>
                                                     </div>
-                                                    <div className="opacity-0 group-hover:opacity-100 flex items-center gap-1 transition-opacity">
+                                                    <div className="opacity-0 group-hover:opacity-100 flex items-center gap-1 transition-opacity shrink-0 ml-2">
                                                         {task.status === 'failed' && (
                                                             <button
                                                                 onClick={() => onRetryTask && onRetryTask(repo.name, task.id)}
